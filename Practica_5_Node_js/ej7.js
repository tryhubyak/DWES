function bloqueLet() {
    if (true) {
      let variableLet = "Variable de bloque LET";
      console.log(variableLet);
    }
}
bloqueLet();

function bloqueConst() {
    const variableConst = "Variable constante (const)";
    console.log(variableConst);
}
  
bloqueConst();
  