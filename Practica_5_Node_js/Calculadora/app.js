const { suma, resta } = require('./operaciones');

const resultadoSuma = suma(42, 3);
const resultadoResta = resta(56, 2);

console.log('Suma, resultado:', resultadoSuma);
console.log('Resta, resultado:', resultadoResta);
